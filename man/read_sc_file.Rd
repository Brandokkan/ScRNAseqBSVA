% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-AllGenerics.R, R/read_sc_file.R
\name{read_sc_file}
\alias{read_sc_file}
\alias{read_sc_file,character-method}
\title{Reads one or multiple single cell files and outputs
the corresponding variable containing the information
inside the file(s)}
\usage{
read_sc_file(path, ...)

\S4method{read_sc_file}{character}(path, bar_path, gene_path, ...)
}
\arguments{
\item{path}{the path of the file containing the read counts}

\item{...}{additional parameters}

\item{bar_path}{the path of the .tsv file containing the cells bar-codes}

\item{gene_path}{the path of the .tsv file containing the names of the genes}
}
\value{
a variable containing the information in the file(s)
in the form of a \code{dgCMatrix}.
}
\description{
The output variable can be used for single-cell analysis
}
\section{Functions}{
\itemize{
\item \code{read_sc_file(character)}: Method for creating the corresponding
single-cell variable from a file or multiple files. For .mtx files, a
gene and cell barcodes must also be submitted in .tsv format

}}
\examples{
mat_path <- system.file("extdata", "sc_test.mtx", package = "ScRNAseqBSVA")
gene_path <- system.file("extdata", "genes.tsv", package = "ScRNAseqBSVA")
cell_path <- system.file("extdata", "barcode.tsv", package = "ScRNAseqBSVA")
mtx_mat <- read_sc_file(mat_path,
    gene_path = gene_path, bar_path = cell_path
)
mtx_mat

csv_path <- system.file("extdata", "sc_test.csv", package = "ScRNAseqBSVA")
csv_mat <- read_sc_file(csv_path)
csv_mat
}
