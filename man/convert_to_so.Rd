% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-AllGenerics.R, R/convert_to_so.R
\name{convert_to_so}
\alias{convert_to_so}
\alias{convert_to_so,data.frame-method}
\alias{convert_to_so,character-method}
\alias{convert_to_so,dgCMatrix-method}
\title{Creates a Seurat object for single-cell analysis from a file or
various variable types.}
\usage{
convert_to_so(
  sc_data,
  min.cells = 3,
  min.features = 200,
  rem_ens_name = TRUE,
  ens_reg = "_[^_]*$",
  ...
)

\S4method{convert_to_so}{data.frame}(
  sc_data,
  min.cells = 3,
  min.features = 200,
  rem_ens_name = TRUE,
  ens_reg = "_[^_]*$",
  ...
)

\S4method{convert_to_so}{character}(
  sc_data,
  min.cells = 3,
  min.features = 200,
  rem_ens_name = TRUE,
  ens_reg = "_[^_]*$",
  ...
)

\S4method{convert_to_so}{dgCMatrix}(
  sc_data,
  min.cells = 3,
  min.features = 200,
  rem_ens_name = TRUE,
  ens_reg = "_[^_]*$",
  ...
)
}
\arguments{
\item{sc_data}{Object or path containing the single-cell data. See the Methods
section below for supported input classes.}

\item{min.cells}{A gene must be expressed in at least this number of cells to
be included. Default is \code{min.cells = 3}.}

\item{min.features}{A cell must express at least this number of genes to be
included. Default is \code{min.features = 200}.}

\item{rem_ens_name}{if \code{TRUE}, then an ENSEMBL suffix is assumed to be
present in the gene names and is removed.}

\item{ens_reg}{what regular expression is used to recognize and substitute
the gene name from the ENSEMBL suffix.
Ignored if \code{rem_ens_name} is \code{FALSE}.}

\item{...}{Additional arguments passed to
\code{\link[Seurat]{CreateSeuratObject}}, e.g. \code{project}}
}
\value{
A \code{Seurat} object.
}
\description{
Creates a \code{Seurat} object for downstream single-cell analysis,
dispatching on the class of \code{sc_data}. By default, it filters
cells and genes by min.cells and min.features and removes the ensemble
suffix.
}
\section{Functions}{
\itemize{
\item \code{convert_to_so(data.frame)}: Method for a \code{data.frame} of counts
(genes x cells).

\item \code{convert_to_so(character)}: Method for a file path to a supported
single-cell data file (e.g. a .mtx or .csv file).

\item \code{convert_to_so(dgCMatrix)}: Method for a sparse counts matrix of class
\code{dgCMatrix}.

}}
\examples{
mat_path <- system.file("extdata", "sc_test.mtx", package = "ScRNAseqBSVA")
gene_path <- system.file("extdata", "genes.tsv", package = "ScRNAseqBSVA")
cell_path <- system.file("extdata", "barcode.tsv", package = "ScRNAseqBSVA")
mtx_mat <- convert_to_so(mat_path, gene_path = gene_path, bar_path = cell_path)
mtx_mat

csv_path <- system.file("extdata", "sc_test.csv", package = "ScRNAseqBSVA")
csv_mat <- convert_to_so(csv_path)
csv_mat

tsv_path <- system.file("extdata", "sc_test.tsv", package = "ScRNAseqBSVA")
tsv_df <- read.table(tsv_path, header = TRUE, sep = "\t", row.names = 1)
tsv_mat <- convert_to_so(tsv_df)
tsv_mat
}
\seealso{
\code{\link[Seurat]{CreateSeuratObject}}
}
