% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-AllGenerics.R, R/sc_normalization.R
\name{sc_normalization}
\alias{sc_normalization}
\alias{sc_normalization,Seurat-method}
\title{Normalization of gene counts}
\usage{
sc_normalization(so, loga = TRUE, scale_factor = 10000, ...)

\S4method{sc_normalization}{Seurat}(so, loga = TRUE, scale_factor = 10000, ...)
}
\arguments{
\item{so}{\code{SeuratObject} containing the counts to normalize}

\item{loga}{Boolean value telling the function if it should output the
logarithm of the normalized counts}

\item{scale_factor}{the scale factor that multiplies the division in order
to make it more human readable.}

\item{...}{additional parameters}
}
\value{
the \code{SeuratObject} with the normalized counts
}
\description{
This function normalizes the counts inside a \code{SeuratObject} to
make comparison between different cell expression levels.
The default normalization is counts per ten thousand.
}
\section{Functions}{
\itemize{
\item \code{sc_normalization(Seurat)}: method to normalize the counts

}}
\examples{
data("so_mit_rbp")

norm_so <- sc_normalization(so_mit_rbp)
so_10k <- sc_normalization(so_mit_rbp, loga = FALSE)
so_1 <- sc_normalization(so_mit_rbp, loga = FALSE, scale_factor = 1)

}
