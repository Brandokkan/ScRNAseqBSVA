% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-AllGenerics.R, R/calculate_mt_rbp.R
\name{calculate_mt_rbp}
\alias{calculate_mt_rbp}
\alias{calculate_mt_rbp,Seurat-method}
\title{Adds to a \code{SeuratObject} metadata the percentage of mitochondrial
and ribosomal protein genes per cell.}
\usage{
calculate_mt_rbp(
  so,
  specie = "human",
  mit_pat = "^MT-",
  rib_pat = "^RP[LS]",
  overwrite = FALSE
)

\S4method{calculate_mt_rbp}{Seurat}(
  so,
  specie = "human",
  mit_pat = "^MT-",
  rib_pat = "^RP[LS]",
  overwrite = FALSE
)
}
\arguments{
\item{so}{the \code{SeuratObject} that contains the data to visualize}

\item{specie}{the specie from which the data was derived. Allows to know
the correct nomenclature of the gene names. it can be set as: human, mouse
or other. default value is human.}

\item{mit_pat}{reg-ex pattern for finding the mitochondrial genes.
Only change if \code{specie} is set to "other".}

\item{rib_pat}{reg-ex pattern for finding the ribosomal protein genes.
Only change if \code{specie} is set to "other".}

\item{overwrite}{logical value to tell the function if it should overwrite
the current values of mitochondrial and ribosomal genes. If set to
\code{TRUE} while the values are not yet calculated, it raises an error.}
}
\value{
the \code{SeuratObject} with the new metadata
}
\description{
This function calculates and adds to the \code{SeuratObject} metadata, if
not already present, the percentage of mitochondrial and ribosomal protein
genes.
}
\section{Functions}{
\itemize{
\item \code{calculate_mt_rbp(Seurat)}: method to calculate the mitochondrial and
ribosomal percentages.

}}
\examples{
csv_path <- system.file("extdata", "sc_test.csv", package = "ScRNAseqBSVA")

so_mit_rbp <- convert_to_so(csv_path)
so_mit_rbp <- calculate_mt_rbp(so_mit_rbp, specie = "mouse")

}
